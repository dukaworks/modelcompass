# 🧭 ModelCompass 项目架构文档

**项目名称**: ModelCompass (魔盘)  
**版本**: 0.1.0 MVP  
**定位**: AI模型画像与智能选型平台

---

## 📋 项目概述

ModelCompass 帮助开发者在 AI 模型的海洋中找到最适合的选择。通过聚合全球模型数据（OpenRouter、HuggingFace 等），结合社区反馈和自动化评测，提供：

1. **模型画像库** - 能力、价格、性能全维度数据
2. **智能选型** - 基于场景自动推荐最优模型组合
3. **API 代理** - 统一接口调用全球模型
4. **社区 Wiki** - 众包共建模型评价体系

---

## 🏗️ 技术架构

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Web界面    │  │   Dify插件   │  │   API调用    │      │
│  │  (Next.js)   │  │   (未来)     │  │   (现在)     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      前端层 (Vercel)                         │
│  Next.js 14 + Tailwind CSS + TypeScript                     │
│  - 模型展示页面                                             │
│  - 智能选型表单                                             │
│  - 管理面板 (未来)                                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      API层 (Railway)                         │
│  Node.js + Express + TypeScript                             │
│  ┌────────────┬────────────┬────────────┬────────────┐     │
│  │  /models   │  /proxy    │ /recommend │  /admin    │     │
│  │  模型查询  │  API代理   │  智能推荐  │  爬虫控制  │     │
│  └────────────┴────────────┴────────────┴────────────┘     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据层 (PostgreSQL)                       │
│  Prisma ORM                                                 │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐           │
│  │   Model    │  │ Heartbeat  │  │  ApiUsage  │           │
│  │  模型画像  │  │  任务日志  │  │  使用统计  │           │
│  └────────────┘  └────────────┘  └────────────┘           │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    外部数据源                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  OpenRouter  │  │ HuggingFace  │  │  官方文档    │      │
│  │  (价格/API)  │  │  (模型信息)  │  │  (能力描述)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 项目结构

```
modelcompass/
├── README.md                  # 项目介绍
├── Dockerfile                 # 生产构建配置
├── docker-compose.yml         # 本地开发环境
├── railway.json               # Railway部署配置
├── docs/
│   ├── ARCHITECTURE.md        # 本文档
│   └── DEPLOY.md              # 部署指南
│
├── backend/                   # 后端服务
│   ├── Dockerfile             # 后端容器配置
│   ├── package.json           # 依赖管理
│   ├── tsconfig.json          # TypeScript配置
│   ├── .env.example           # 环境变量模板
│   │
│   ├── prisma/
│   │   ├── schema.prisma      # 数据库模型定义
│   │   └── seed.ts            # 种子数据
│   │
│   └── src/
│       ├── index.ts           # 应用入口
│       ├── heartbeat-runner.ts # 心跳任务启动器
│       │
│       ├── routes/            # API路由
│       │   ├── models.ts      # 模型CRUD
│       │   ├── proxy.ts       # API代理
│       │   ├── recommend.ts   # 智能推荐
│       │   ├── admin.ts       # 管理接口
│       │   └── heartbeat.ts   # 心跳控制
│       │
│       ├── services/          # 业务逻辑
│       │   ├── crawler.ts     # OpenRouter爬虫
│       │   ├── hf-crawler.ts  # HuggingFace爬虫
│       │   ├── heartbeat.ts   # 心跳调度器
│       │   ├── social-scanner.ts  # 社交媒体扫描
│       │   └── benchmark-crawler.ts # 评测数据抓取
│       │
│       ├── crawler/           # 爬虫CLI工具
│       │   ├── index.ts
│       │   ├── openrouter.ts
│       │   └── cli.ts
│       │
│       └── utils/
│           └── db.ts          # Prisma客户端
│
├── frontend/                  # 前端应用
│   ├── package.json
│   ├── next.config.js
│   ├── tailwind.config.js
│   ├── tsconfig.json
│   │
│   ├── app/                   # Next.js App Router
│   │   ├── layout.tsx
│   │   ├── page.tsx           # 首页
│   │   └── globals.css
│   │
│   └── components/            # React组件
│       ├── ModelCard.tsx      # 模型卡片
│       └── RecommendForm.tsx  # 推荐表单
│
└── shared/                    # 共享类型(预留)
```

---

## 🔧 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| **前端** | Next.js 14 | React生态成熟，SSR/SSG支持，Vercel一键部署 |
| **样式** | Tailwind CSS | 快速开发，响应式，维护性好 |
| **后端** | Express + TS | 轻量灵活，TypeScript类型安全 |
| **数据库** | PostgreSQL | 开源稳定，JSON支持好，Railway免费 |
| **ORM** | Prisma | 类型安全，迁移方便，开发体验好 |
| **爬虫** | Axios+Cheerio | 轻量高效，熟悉度高 |
| **部署** | Railway+Vercel | 免费额度够用，Git推送自动部署 |

---

## 📊 数据模型

### Model (模型画像)
```typescript
{
  id: string              // 唯一ID
  modelId: string         // 模型标识 (如 "gpt-4o")
  name: string            // 显示名称
  provider: string        // 提供商
  description?: string    // 描述
  capabilities: string[]  // 能力标签 [chat, vision, code]
  contextLength: number   // 上下文长度
  maxTokens?: number      // 最大输出token
  promptPrice: number     // 输入价格 ($/1K tokens)
  completionPrice: number // 输出价格 ($/1K tokens)
  tags: string[]          // 标签 [热门, 免费, 中文]
  recommendedFor: string[] // 推荐场景
  codingScore?: number    // 代码能力评分 (0-100)
  reasoningScore?: number // 推理能力评分
  writingScore?: number   // 写作能力评分
  chineseScore?: number   // 中文能力评分
  speedScore?: number     // 响应速度评分
  websiteUrl?: string     // 官网链接
  docsUrl?: string        // 文档链接
  isActive: boolean       // 是否启用
  usageCount: number      // 使用次数
  ratingAvg?: number      // 平均评分
  ratingCount: number     // 评分数量
  createdAt: DateTime
  updatedAt: DateTime
}
```

### HeartbeatLog (心跳任务日志)
```typescript
{
  id: string
  taskName: string        // 任务名
  status: string          // success | failed | running
  error?: string          // 错误信息
  duration?: number       // 执行时间(ms)
  timestamp: DateTime
}
```

### ApiUsage (API调用统计)
```typescript
{
  id: string
  modelId: string
  provider: string
  promptTokens: number
  completionTokens: number
  totalTokens: number
  costUsd: number
  latencyMs: number
  status: string
  createdAt: DateTime
}
```

---

## 🚀 API 路由

### 公开路由

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/health` | 健康检查 |
| GET | `/api/models` | 获取模型列表 (支持过滤) |
| GET | `/api/models/:id` | 获取单个模型详情 |
| POST | `/api/proxy/chat` | 代理聊天请求 |
| POST | `/api/recommend` | 智能推荐模型 |

### 管理路由

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/admin/crawl` | 触发爬虫 (source: openrouter/huggingface) |
| GET | `/api/admin/stats` | 获取统计数据 |
| GET | `/api/admin/heartbeat/status` | 心跳任务状态 |
| POST | `/api/admin/heartbeat/start` | 启动心跳 |
| POST | `/api/admin/heartbeat/stop` | 停止心跳 |
| POST | `/api/admin/heartbeat/trigger/:task` | 手动触发任务 |

---

## 💓 心跳系统 (Heartbeat)

### 定时任务调度

```typescript
const tasks = {
  'crawl-openrouter': {
    interval: '1h',      // 每小时
    action: '抓取OpenRouter模型和价格'
  },
  'crawl-huggingface': {
    interval: '6h',      // 每6小时
    action: '抓取HuggingFace热门模型'
  },
  'scan-social': {
    interval: '12h',     // 每12小时
    action: '扫描社交媒体讨论'
  },
  'crawl-benchmarks': {
    interval: '24h',     // 每24小时
    action: '更新评测数据'
  }
}
```

### 启动方式
```bash
# 开发环境
npm run heartbeat:start

# 生产环境 (Docker)
# 自动随服务启动
```

---

## 🐳 本地开发

### 方式1: Docker Compose (推荐)
```bash
# 启动 PostgreSQL + 后端服务
docker-compose up -d postgres

# 初始化数据库
cd backend
npx prisma migrate dev
npm run db:seed

# 启动后端
npm run dev
```

### 方式2: 本地 Node.js
```bash
# 1. 安装 PostgreSQL (或 Docker)
docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=password postgres:15

# 2. 配置环境变量
cp backend/.env.example backend/.env
# 编辑 DATABASE_URL

# 3. 安装依赖并启动
cd backend
npm install
npx prisma migrate dev
npm run db:seed
npm run dev
```

---

## 🚀 部署流程

### Railway (后端+数据库)

1. 登录 https://railway.app
2. 创建项目 → Deploy from GitHub Repo
3. 选择 `dukaworks/modelcompass`
4. Add PostgreSQL Database
5. 配置环境变量: `OPENROUTER_API_KEY`
6. 自动部署完成

### Vercel (前端)

1. 登录 https://vercel.com
2. Import Project → 选择 modelcompass
3. 根目录: `frontend`
4. 环境变量: `NEXT_PUBLIC_API_URL=https://your-railway-url.up.railway.app`
5. Deploy

---

## 📈 MVP 功能清单

### ✅ 已完成
- [x] 后端 API 框架 (Express + TypeScript)
- [x] 数据库模型 (Prisma + PostgreSQL)
- [x] 基础路由 (models, proxy, recommend, admin)
- [x] OpenRouter 爬虫
- [x] HuggingFace 爬虫
- [x] 心跳调度系统
- [x] Next.js 前端界面
- [x] Docker 配置
- [x] Railway 部署配置

### 🚧 待开发
- [ ] 用户认证系统
- [ ] 前端管理面板
- [ ] 真实社交媒体扫描
- [ ] Benchmark 数据抓取
- [ ] Dify 插件集成
- [ ] 高级推荐算法

---

## 🔐 环境变量

### 必需
```bash
DATABASE_URL="postgresql://..."
OPENROUTER_API_KEY="sk-or-v1-..."
PORT=3001
NODE_ENV=production
```

### 可选
```bash
REDIS_URL="redis://..."
SILICONFLOW_API_KEY="..."
```

---

## 📄 许可证

MIT License - 自由使用和修改

---

**Made with 🦞 by Duka & 小龙虾**

Last Updated: 2025-01-21
